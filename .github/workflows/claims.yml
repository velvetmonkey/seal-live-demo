# SPDX-License-Identifier: Apache-2.0
name: claims-drift
on:
  push:
  pull_request:
jobs:
  claims:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/claims-drift.mjs
      - run: node scripts/check_provenance_surfaces.mjs
